# Keyboard
setxkbmap us -option compose:ralt

# Polkit
/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
eval `ssh-agent -s` &
ssh-add ~/.ssh/xiaomi-air &

# Conman
connman-gtk --tray &

# Blueman
blueman-applet &

# Function keys
xbindkeys &
